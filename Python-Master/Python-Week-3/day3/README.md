# Day 3: Queues and Deques

## Overview
A Queue is a First-In, First-Out (FIFO) structure. Think of a line at a grocery store. Using a Python `list` for a queue is terrible (`pop(0)` is $O(N)$). You must use `collections.deque`.

## Structure
- `notes/`: `deque` internals and BFS preview.
- `drills/`: Queue mechanisms and Sliding Window Maximums.
- `platform_problems/`: Queue specific problems.
